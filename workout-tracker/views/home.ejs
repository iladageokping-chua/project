<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Workout Tracker</title>
    <style>
        body {
            background: url('/images/pexels-victorfreitas-841130.jpg') no-repeat center center fixed;
            background-size: cover;
            padding-top: 70px;
        }
        /* ตั้งค่า Reset */
        body, h1, p, a {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        .navbar {
            position: fixed; /* ทำให้ Navbar อยู่คงที่ */
            top: 0;
            left: 0;
            width: 100%;
            background: #222;
            padding: 15px 20px;
            color: white;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 1000; 
        }

        .navbar .logo {
            display: flex;
            align-items: center;
        }

        .navbar .logo img {
            width: 50px;
            margin-right: 10px;
            border-radius: 10px;
        }

        .navbar .menu a {
            color: white;
            text-decoration: none;
            margin-left: 15px;
        }
        .menu {
            display: flex;
            align-items: center;
            gap: 3px;
            margin-left: auto;
            padding-right: 20px;      
        }

        .menu ul {
            display: flex;
            padding: 0;
            margin: 0;
            list-style: none;
}

        .navbar .menu a:hover {
            color: white;
            text-decoration: underline;
        }

        .navbar .btn {
            background: #007bff;
            padding: 8px 12px;
            border-radius: 10px;
        }

        .bth {
            background-color: #0066be;
            color: white;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 5px;
            transition: background 0.3s ease;
        }

        .bth:hover {
            background-color: #4199fd; 
        }

        /* สไตล์ Hero Section */
        .hero {
            position: relative;
            width: 100%;
            height: 100vh; 
            background: url('/images/workout-bg.jpg') center/cover no-repeat;
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
            text-align: left;
        }

        .hero .overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
        }

        .hero-text {
            position: relative;
            z-index: 2; 
        }

        .hero h1 {
            font-size: 4em;
            font-weight: bold;
        }

        .hero p {
            font-size: 2em;
        }

        /* ตั้งค่าให้รายการในเมนูแสดงในแนวนอน */
        ul {
            list-style-type: none;
            margin: 0;
            padding: 0;
        }

        li {
            display: inline-block;
            margin-right: 20px;
        }

    </style>

</head>
<body>
    <nav class="navbar">
        <div class="logo">
            <img src="/images/iconweb.png" alt="Logo">
            <div>Workout Tracker</div>
        </div>
        <div class="menu">
            <ul>
              <li><a href="/login" >Log In</a></li>
                <li><a href="/signup" class="bth">Sign up</a></li>
            </ul>
        </div>
    </nav>

    <section class="hero">
        <div class="overlay"></div>
        <div class="hero-text">
            <h1>You are Stronger Than You Think!</h1>
            <br> <p>For Beginners, Athletes</p>
        </div>
    </section>

</body>
</html>
