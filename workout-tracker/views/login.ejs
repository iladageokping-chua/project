<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>

    <style>
        /* จัดตำแหน่งของ body และ html */
        body, html {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            height: 100%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            background-color: #f4f4f4;
        }

        /* แถบข้างบนที่มีข้อความ Workout Tracker อยู่ทางซ้าย */
        header {
            width: 100%;
            background-color: #333;
            color: white;
            padding: 20px;
            position: fixed;
            top: 0;
            left: 0;
            display: flex;
            align-items: center;
            font-size: 24px;
            font-weight: bold;
        }

        /* กำหนดพื้นที่สำหรับฟอร์มล็อกอิน */
        .login-container {
            margin-top: 100px; /* เพื่อไม่ให้ฟอร์มทับแถบ */
            width: 100%;
            max-width: 400px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            padding: 30px;
            text-align: center;
        }

        /* กำหนดการจัดข้อความในฟอร์ม */
        .login-form h2 {
            font-size: 24px;
            margin-bottom: 20px;
            color: #333;
        }

        /* ฟอร์ม input */
        .login-form input {
            width: 100%;
            padding: 15px;
            margin: 10px 0;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 16px;
            background-color: #f0f0f0;
            box-sizing: border-box;
        }

        /* เมื่อโฟกัสที่ช่องกรอกข้อมูล */
        .login-form input:focus {
            outline: none;
            border-color: #007bff;
            background-color: #fff;
        }

        /* ปุ่ม Login */
        .login-form button {
            width: 100%;
            padding: 15px;
            background-color: #333;
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 18px;
            cursor: pointer;
            margin-top: 10px;
            transition: background-color 0.3s;
        }

        /* เปลี่ยนสีของปุ่มเมื่อ hover */
        .login-form button:hover {
            background-color: #0056b3;
        }

        .signup-text {
    font-size: 16px;
    color: #333;
    margin-bottom: 10px;
    text-align: center;
}

        /* ปุ่ม Sign Up */
.back-button {
    background-color:none;
            color: blue;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 5px;
            transition: color 0.3s ease, border-bottom 0.3s ease;
}

/* เปลี่ยนสีของ Sign Up เมื่อ hover */
.back-button:hover {
    color: #0066be; /* เปลี่ยนสีตัวอักษรเป็นน้ำเงินเมื่อ hover */
    border-bottom: 2px solid #0066be; /* เพิ่มขีดเส้นใต้สีน้ำเงินเมื่อ hover */
    box-shadow: 0 6px 15px rgba(0, 0, 0, 0.2); /* เพิ่มเงาเมื่อ hover */
}

       /* ปุ่ม Back to Home */
.button-common {
    color: #000000;
    text-decoration: none;
    font-size: 18px;
    font-weight: bold;
    padding: 12px 25px;
    border-radius: 5px;
    display: inline-block;
    transition: background-color 0.3s, transform 0.3s ease;
    margin-top: 10px;
    border: none;
}

/* เปลี่ยนสีเมื่อ hover */
.button-common:hover {

    transform: scale(1.05); /* เพิ่มขนาดเล็กน้อยเมื่อ hover */
}

    </style>
</head>
<body>
    <header>
        Workout Tracker
    </header>
    <div class="login-container">
        <main>
            <section class="login-form">
                <h2>Log In</h2>
        
                <% if (error) { %>
                    <p style="color: red;"><%- error %></p>
                <% } %>
        
                <form method="POST" action="/login" autocomplete="off">
                    <label for="username">Username</label>
                    <input type="text" name="username" id="username" required autocomplete="off"><br><br>
        
                    <label for="password">Password</label>
                    <input type="password" name="password" id="password" required autocomplete="new-password"><br><br>
        
                    <button type="submit">Log In</button>
                </form>
                <br>
                <br>
                <div class="signup-text">
                    Don't have an account? <br>
                    <a href="/signup" class="back-button">Sign Up</a><br><br>
                    <a href="/" class="button-common">Back to Home</a>
                </div>
            </section>
        </main>
    </div>

    <script>
        document.getElementById('loginForm').addEventListener('submit', function(e) {
            e.preventDefault();
            // รับข้อมูลจากฟอร์ม
            const username = document.getElementById('username').value;
            const password = document.getElementById('password').value;
            // สร้างออบเจ็กต์ JSON สำหรับส่งข้อมูล
            const loginData = {
                username: username,
                password: password
            };
            // ส่งข้อมูลไปยังเซิร์ฟเวอร์ด้วย fetch API
            fetch('/login', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(loginData)
            })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    window.location.href = '/workout_tracker';
                } else {
                    alert(data.message);
                }
            })
            .catch(error => {
                console.error('Error:', error);
            });
        });
    </script>
</body>
</html>
